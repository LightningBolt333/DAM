# Navigation

## Flow
1. **App Launch**: The user opens the app.
2. **Main Screen**: Automatically triggers the first API call to fetch 20 random dog images.
3. **Refresh**: User performs a "Swipe-to-refresh" to trigger a new API call and replace the current list.
